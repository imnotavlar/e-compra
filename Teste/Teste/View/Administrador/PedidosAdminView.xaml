<UserControl x:Class="Teste.View.PedidosAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8FAFC">

    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="RowHeight" Value="58"/>
            <Setter Property="SelectionMode" Value="Single"/>

            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F1F5F9"/>
                        <Setter Property="Foreground" Value="#64748B"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="16,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Foreground" Value="#334155"/>
                    </Style>
                </Setter.Value>
            </Setter>

            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F1F5F9"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8FAFC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#F0F9FF"/>
                                <Setter Property="Foreground" Value="#0369A1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnAcaoStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnVisualizarStyle" TargetType="Button">
            <Setter Property="Background" Value="#EFF6FF"/>
            <Setter Property="Foreground" Value="#2563EB"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnEnviarRotaStyle" TargetType="Button">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Gerenciamento de Pedidos (Painel Admin)" FontSize="26" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock Text="Acompanhe, agende rotas e atualize os pedidos em tempo real." FontSize="14" Foreground="#64748B" Margin="0,5,0,0"/>

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <TextBlock x:Name="AbaPendentesTexto" Text="Pedidos Pendentes" MouseLeftButtonDown="AbaPendentes_Click" Cursor="Hand" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,24,0"/>
                    <TextBlock x:Name="AbaCaminhoTexto" Text="A Caminho (Rota do Dia)" MouseLeftButtonDown="AbaCaminho_Click" Cursor="Hand" FontWeight="Normal" Foreground="#94A3B8" Margin="0,0,24,0"/>
                    <TextBlock x:Name="AbaHistoricoTexto" Text="Histórico de Pedidos" MouseLeftButtonDown="AbaHistorico_Click" Cursor="Hand" FontWeight="Normal" Foreground="#94A3B8"/>
                </StackPanel>
            </StackPanel>

            <StackPanel x:Name="PainelFiltroData" Grid.Column="1" VerticalAlignment="Bottom" Visibility="Collapsed">
                <TextBlock Text="Filtrar por Data de Entrega:" FontSize="12" FontWeight="Medium" Foreground="#64748B" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal">
                    <DatePicker x:Name="FiltroCalendario" Width="150" Height="35" SelectedDateChanged="FiltroCalendario_SelectedDateChanged"/>
                    <Button Content="Limpar" Click="LimparFiltro_Click" Margin="8,0,0,0" Padding="10,0" Style="{StaticResource BtnVisualizarStyle}" Height="35"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="6" BorderBrush="#E2E8F0" BorderThickness="1">
            <DataGrid x:Name="GridPedidos" ItemsSource="{Binding ListaPedidosPendentes}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Código" Binding="{Binding NomePedido}" Width="130" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Cliente" Binding="{Binding Recebedor}" Width="130"/>

                    <DataGridTemplateColumn Header="Composição" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border x:Name="BadgeBorder" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock x:Name="BadgeText" Text="{Binding TipoComposicao}" FontSize="11" FontWeight="Bold"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding TipoComposicao}" Value="Completa">
                                        <Setter TargetName="BadgeBorder" Property="Background" Value="#E0F2FE"/>
                                        <Setter TargetName="BadgeText" Property="Foreground" Value="#0369A1"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TipoComposicao}" Value="Modificada">
                                        <Setter TargetName="BadgeBorder" Property="Background" Value="#FFEDD5"/>
                                        <Setter TargetName="BadgeText" Property="Foreground" Value="#C2410C"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TipoComposicao}" Value="Padrão">
                                        <Setter TargetName="BadgeBorder" Property="Background" Value="#F1F5F9"/>
                                        <Setter TargetName="BadgeText" Property="Foreground" Value="#475569"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Montagem" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border x:Name="MontagemBorder" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed">
                                    <TextBlock x:Name="MontagemText" Text="Pronta" FontSize="11" FontWeight="Bold"/>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding StatusMontagem}" Value="Pronta">
                                        <Setter TargetName="MontagemBorder" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="MontagemBorder" Property="Background" Value="#DBEAFE"/>
                                        <Setter TargetName="MontagemText" Property="Foreground" Value="#1E40AF"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Detalhes" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Observacoes}" Foreground="#64748B" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Data Entrega" Binding="{Binding DataEntrega, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>

                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='R$ {0:N2}'}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#0F172A"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>

                    <DataGridTemplateColumn Header="Ações" Width="260">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Button Content="Ver Itens" Style="{StaticResource BtnVisualizarStyle}" Click="VerItens_Click" Margin="0,0,12,0"/>

                                    <Button x:Name="BtnAgendar" Content="Enviar p/ Rota" Click="AgendarRota_Click" Margin="0,0,12,0">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource BtnEnviarRotaStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pendente">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <Button x:Name="BtnConcluir" Content="Entregue" Click="MarcarEntregue_Click">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource BtnAcaoStyle}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="A Caminho">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>